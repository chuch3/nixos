Place your generated `hardware-configuration.nix` here.

Use `nixos-generate-config --show-hardware-config` and `nixos-hardware` to 
find modules based on your specified hardware. Place it inside the 
`platform.nix` and add nixos options in `options.nix`.
