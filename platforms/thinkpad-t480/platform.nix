# Thinkpad T480
{
  platform = {
    type = "x86_64-linux";

    # TODO: Add hardware specific configurations with `.mkEnableOption`
    # features (mainly graphics) available on this system
    /*
    available-features = {
      vsync = true;
      gamma-ramp = true;
      dp-alt-mode = true;
    };
    */
  };
}
